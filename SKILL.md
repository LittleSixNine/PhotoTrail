---
name: geotag-photos
description: "从带定位照片和已有 GPX 合并生成新 GPX，或根据指定 GPX 给缺少定位的照片补写 GPS；两个流程严格分开，支持相机时间校正与胶片时间缺失检查。"
---
# 照片定位与轨迹整理

## 操作分流：先确定本次做哪件事

| 模式 | 输入 | 输出 | 不得执行 |
| --- | --- | --- | --- |
| A：生成 GPX | 带 GPS 的照片、已有 GPX，任选或组合 | 新 GPX、来源与异常记录 | 修改任何照片元数据；覆盖输入轨迹；自动进入模式 B |
| B：给照片补 GPS | 明确指定的 GPX、待处理照片 | 补写结果、匹配记录、备份 | 从手机照片生成轨迹；合并或改写 GPX；自动进入模式 A |

- “合并轨迹”“从手机照片导出 GPX”进入 A；“用这个 GPX 给相机照片补定位”进入 B。
- 只说“处理定位”且无法从上下文判断时，询问模式；可先只读检查资料。
- 用户明确同时要求两件事时，按 A、B 两个独立步骤执行。A 完成并验证后，以其确定的输出路径作为 B 的输入，分别报告结果；已有授权不重复询问。
- B 缺少可用 GPX 时报告缺失，不自行切换 A。完成 A 不代表获得照片写入授权。
- 不把“生成新 GPX”描述为已给照片添加 GPS，也不把连起来的稀疏定位点描述为完整真实路线。

## 共用的数据规则

- 批量读取和计算在本地工具中完成，模型只接收统计、参数和必要异常，避免把全部 EXIF、轨迹点或照片送入上下文。
- 统一到 UTC 后比较时间，保留原始时间、时区及校正依据。照片拍摄时间、GPS 定位时间和文件修改时间不得混用。
- 优先采用有效且与拍摄时间一致的 GPS 时间；否则使用带明确时区的实际拍摄时间。时区不明或时间冲突时，标记并排除依赖该时间的自动匹配，不猜测。
- 数码相机按设备处理时区和时钟偏差；有可靠同步点时才校正漂移。记录偏移方向及一组校正前后时间示例，不把时区偏移重复叠加。
- 胶片扫描时间不是拍摄时间。只有日期、底片顺序或扫描时间时，不自动赋予精确位置；记录为缺少拍摄时间，使用用户提供的时间映射后再匹配。
- 检查坐标范围和明显异常跳点；(0, 0) 等可疑值需结合上下文判断。不同来源的坐标参考系若不明确，不能直接混合；输出 GPX 使用 WGS 84。
- 自动补写过的照片不得默认为新的实测来源，以免推算位置反复传播。可识别时标记来源；来源不明时说明无法验证。

## 模式 A：生成新 GPX

### 范围与输入

读取用户指定的带定位照片和/或一个以上 GPX；明确输出位置。不得默认为整个照片库递归扫描。目标文件存在时另取名称，保留全部输入。

### 处理

1. 从照片提取有效位置和对应时间，从 GPX 提取有效的带时间轨迹点；记录源文件、原始轨迹段、时间字段与校正依据。
2. 统一时间后排序，合并重复观测；仅按相同时间和坐标等明确条件去重，不随意合并相近但不同的点。
3. 同一时刻坐标冲突或速度明显异常时列入异常记录。优先参考经检查连续、可信的记录，但不固定认定某类设备一定正确；无法解决的冲突点不进入自动可用轨迹。
4. 保留原轨迹断点；新增长时间缺口也要断段。根据来源采样间隔和活动情境选定断段阈值并记录，不能把步行阈值直接套给乘车行程。
5. 手机照片的稀疏点可放在独立轨迹段中，孤立点允许形成单点段。不得用直线补点、地图寻路或模型猜测伪造未观测路线；不得把无时间点编造时间后混入时间匹配轨迹。
6. 输出 GPX 1.1，使用有效经纬度、UTC 时间和明确轨迹段；只保留确有来源的海拔等附加值，不凭空补全。

### 交付与验证

- 交付新 GPX 和配套来源/异常记录（CSV 或 JSON）：各点来源与原始段、时间修正、去重与排除原因、覆盖范围、缺口及断段阈值。
- 重新解析输出，检查格式、坐标、段内时间顺序、有效点数量及时间范围。无有效点时报告失败，不交付空轨迹冒充成功。
- 明确实测覆盖与缺失时段。到此结束，不读取照片写入脚本，不修改照片。

## 模式 B：按指定 GPX 给照片补 GPS

### 准备与预览

1. 确定目标照片范围、指定 GPX、设备时间校正和备份策略。优先采用已有信息；只询问会影响结果且无法合理推断的缺项。
2. 检查 GPX 的有效点、时间范围和断点；若来自 A，读取其配套记录，避免把已标记的缺口或冲突当作可靠覆盖。
3. 默认只处理缺 GPS 的照片。已有有效 GPS 的照片跳过；部分损坏的 GPS 列为异常，不擅自覆盖。
4. 生成匹配预览：文件、原拍摄时间、校正时间、拟写入坐标、对应轨迹段、附近点的时间距离、匹配方法和跳过原因。
5. 仅在同一连续轨迹段内、且满足已记录的最大时间间隔时插值。不得跨断点插值或对范围外时间自动外推。稀疏手机定位点须使用更保守的匹配规则，不能仅因文件是 GPX 就视为连续轨迹。
6. 无可靠时间、时间超限、来源冲突或缺口中的照片保留原状。不要为了提高成功率自动放宽条件。

### 写入与验证

- 写入前说明具体目标、拟写入数量、时间校正、匹配限制、备份位置，以及会修改照片元数据。用户已明确授权且这些条件已确定时直接执行，不重复确认；缺少授权时以预览作为具体确认材料。
- 默认创建备份；只有用户明确要求才关闭备份。保留原始拍摄时间，本流程的时间校正默认只用于匹配。
- 使用 ExifTool 或经过检查的既有脚本，仅对通过预览筛选的文件写 GPS。确认工具实际遵守轨迹断点和间隔限制，必要时使用预先计算的坐标逐张写入；不能假定底层工具自动尊重 GPX 的分段。
- 按文件格式检查写入支持，不声称所有 RAW 均可直接写入。遇到不支持的格式，报告并提出 sidecar 方案，不静默改用另一种交付方式。
- 写入后重新读取 GPS，与拟写入结果核对；检查原始拍摄时间保留、备份存在。汇报成功、跳过、失败数量及文件，附匹配记录和恢复方式；不得把命令运行结束当作全部成功。

## 现有工具与能力边界

现有工具源目录（不得修改或复制到 Codex 目录）：

`/Users/liuziyuan/.cc-switch/skills/geotag-photos-skill`

- `geotag_photos.sh`
- `geotag_photos_v2.sh`
- `quick_geotag.sh`
- 源说明：`README.md`、`skill.yaml`

仅在模式 B 需要使用时读取源说明及相关脚本。依赖 `exiftool`，先检查可用性；缺失时报告，可按授权通过 Homebrew 安装。

源说明描述了按日期分组、GPX 匹配、时间偏移和备份。调用前核实当前脚本的参数、偏移符号、文件筛选及是否覆盖已有 GPS；不以文件名或说明代替检查。其文件命名要求不是本 Skill 对所有实现的强制要求。

模式 A 的多来源合并与两个模式的新增筛选规则是本 Skill 的执行要求，不代表上述脚本已经实现。执行具体任务时优先复用合适工具，以本地确定性处理完成缺失步骤，并进行对应验证；不得向用户宣称已有可直接运行的新程序。

需要修改源脚本时，先取得用户对具体修改的明确确认。修改本 Skill 按用户关于持久指令的审阅规则执行。
