# GPS 照片工具链依赖
# 核心依赖：exiftool（需单独安装）
#   macOS: brew install exiftool
#   Ubuntu: sudo apt-get install libimage-exiftool-perl

# Python 依赖（可选，备用方案）
# pyexiv2>=2.8.0; platform_system=="Linux"
# piexif>=1.1.3
# Pillow>=9.0.0
