# 更新说明

## v0.2 — 2026-09-12

PhotoTrail 首个公开版本。按拍摄时间，为不同设备拍摄的照片补充 GPS。

- 从带 GPS 的照片提取 GPX，逐张处理时区，按拍摄当地日期分组。
- 分析 GPX 轨迹，并生成离线交互预览；默认超过 15 秒才考虑额外推理。
- 对稀疏区间尝试受约束的曲线，保留原始记录点，跳过长断点和异常位置。
- 按指定时间或照片目录估算位置，只给新目录中的照片副本写入 GPS，并回读验证。
- 提供合成轨迹示例、标准库测试和完整使用文档，代码以 MIT 许可发布。

道路匹配目前仅支持生成离线请求草稿，尚未上传或接入返回结果。15 秒是可调初始值，真实步行精度仍待验证。胶片扫描件需先补录真实拍摄时间。
